# ENUM/상태값 사전 (Value Dictionary)

> 컬럼별 가능한 값과 의미 정리

---

## 🔘 공통 컬럼

### del_yn (삭제 여부)
> 대부분의 테이블에서 사용되는 소프트 삭제 플래그

| 값 | 의미 | 비고 |
|----|------|------|
| `'N'` | 활성 (삭제되지 않음) | **기본값**, 조회 시 필터 필수 |
| `'Y'` | 삭제됨 | 조회에서 제외 |

**사용 예시**:
```sql
WHERE del_yn = 'N'  -- 거의 모든 조회에 필수
```

---

### _yn 계열 (Y/N 플래그)
> 여러 테이블에서 공통으로 사용되는 Y/N 플래그

| 컬럼 | Y | N | 기본값 |
|------|---|---|--------|
| `show_yn` | 노출함 | 숨김 | N |
| `hide_yn` | 숨김 | 노출함 | N |
| `use_yn` | 사용함 | 미사용 | N |
| `view_yn` | 노출함 | 미노출 | N |

---

## 👤 member (회원)

### del_yn
| 값 | 의미 |
|----|------|
| `'N'` | 활성 회원 |
| `'Y'` | 탈퇴 회원 |

### sex (성별)
| 값 | 의미 |
|----|------|
| `'M'` | 남성 (Male) |
| `'F'` | 여성 (Female) |

### app_device (앱 디바이스)
| 값 | 의미 |
|----|------|
| `'ios'` | iOS (아이폰) |
| `'android'` | Android |

### subscription_yn (구독 여부)
| 값 | 의미 |
|----|------|
| `'Y'` | 구독 중 |
| `'N'` | 미구독 |

### kakao_yn, push_yn, sms_yn, email_yn (동의 여부)
| 값 | 의미 |
|----|------|
| `'Y'` | 동의함 |
| `'N'` | 동의 안함 |

### marketing_yn (마케팅 동의)
| 값 | 의미 |
|----|------|
| `'Y'` | 마케팅 수신 동의 |
| `'N'` | 마케팅 수신 거부 |

### role (관리자 권한) - admin 테이블
| 값 | 의미 |
|----|------|
| `'admin'` | 관리자 |
| `'super'` | 슈퍼관리자 |

---

## 📦 order (주문)

### status (주문 상태)
| 값 | 의미 | 설명 |
|----|------|------|
| `'0'` | 출고보류 | 출고 대기 상태 |
| `'1'` | 주문완료 | 결제 완료, 처리 대기 |
| `'2'` | 결제취소 | 결제 취소됨 |
| `'3'` | 발송준비 | 상품 포장 중 |
| `'4'` | 배송시작 | 배송사 인계 완료 |
| `'5'` | 배송중 | 배송 진행 중 |
| `'6'` | 배송완료 | 배송 완료 |
| `'7'` | 환불 | 환불 처리됨 |

**사용 예시**:
```sql
-- 결제 완료된 주문
WHERE status = '1'

-- 배송 중인 주문
WHERE status IN ('4', '5')

-- 정상 주문 (취소/환불 제외)
WHERE status NOT IN ('2', '7')
```

### delivery_type (배송 유형)
| 값 | 의미 |
|----|------|
| `'새벽'` | 새벽 배송 |
| `'당일'` | 당일 배송 |
| `'일반'` | 일반 배송 |

### entrance_type (현관 출입 방법)
| 값 | 의미 |
|----|------|
| `'password'` | 비밀번호 |
| `'card'` | 카드키 |
| `'phone'` | 전화 연락 |

### dawn_shipping_area_yn (새벽배송 가능 여부)
| 값 | 의미 |
|----|------|
| `'Y'` | 새벽배송 가능 지역 |
| `'N'` | 새벽배송 불가 지역 |

---

## 📝 order_detail (주문 상세)

### subscribe_yn (구독 상품 여부)
| 값 | 의미 |
|----|------|
| `'Y'` | 구독 상품 |
| `'N'` | 일반 상품 |

### cancel_yn (취소 여부)
| 값 | 의미 |
|----|------|
| `'Y'` | 취소됨 |
| `'N'` | 정상 |

### refund_yn (환불 여부)
| 값 | 의미 |
|----|------|
| `'Y'` | 환불됨 |
| `'N'` | 정상 |

### stock_yn (재고 차감 여부)
| 값 | 의미 |
|----|------|
| `'Y'` | 재고 차감됨 |
| `'N'` | 재고 미차감 |

---

## 🎁 gift_history (사은품)

### gift_type (지급 유형)
| 값 | 의미 |
|----|------|
| `'manual'` | 수동 지급 |
| `'auto'` | 자동 지급 |

### status (상태)
| 값 | 의미 |
|----|------|
| `'given'` | 지급됨 |
| `'cancelled'` | 취소됨 |

---

## 🛍️ items (상품)

### view_yn (노출 여부)
| 값 | 의미 |
|----|------|
| `'Y'` | 판매 중 (노출) |
| `'N'` | 미노출 (숨김) |

### subscribe_yn (구독 가능 여부)
| 값 | 의미 |
|----|------|
| `'Y'` | 구독 가능 |
| `'N'` | 구독 불가 (단건만) |

### dawn_shipping_yn (새벽배송 가능)
| 값 | 의미 |
|----|------|
| `'Y'` | 새벽배송 가능 |
| `'N'` | 새벽배송 불가 |

### item_fresh (신선 상품)
| 값 | 의미 |
|----|------|
| `'Y'` | 신선식품 |
| `'N'` | 일반상품 |

### coupon_useable (쿠폰 사용 가능)
| 값 | 의미 |
|----|------|
| `'Y'` | 쿠폰 적용 가능 |
| `'N'` | 쿠폰 적용 불가 |

### sale_limit_yn (판매 제한)
| 값 | 의미 |
|----|------|
| `'Y'` | 판매 제한 있음 |
| `'N'` | 제한 없음 |

---

## 📋 item_memo (상품 메모)

### memo_type (메모 유형)
| 값 | 의미 |
|----|------|
| `'CS_POINT'` | CS 포인트 |
| `'PRODUCT_LOG'` | 상품 로그 |
| `'VOC_CAUTION'` | VOC 주의사항 |

### priority (우선순위)
| 값 | 의미 |
|----|------|
| `'HIGH'` | 높음 |
| `'MEDIUM'` | 보통 |
| `'LOW'` | 낮음 |

---

## 🔁 subscribe (구독)

### subscribe_yn (구독 활성 여부)
| 값 | 의미 |
|----|------|
| `'Y'` | 구독 활성 |
| `'N'` | 구독 비활성 |

### end_yn (구독 종료 여부)
| 값 | 의미 |
|----|------|
| `'Y'` | 구독 종료됨 |
| `'N'` | 구독 진행 중 |

### auto_coupon_yn (자동 쿠폰 적용)
| 값 | 의미 |
|----|------|
| `'Y'` | 자동 쿠폰 적용 |
| `'N'` | 수동 쿠폰 적용 |

### auto_point_yn (자동 포인트 적용)
| 값 | 의미 |
|----|------|
| `'Y'` | 자동 포인트 적용 |
| `'N'` | 수동 포인트 적용 |

---

## 🎟️ coupon_list (쿠폰)

### type (쿠폰 타입)
| 값 | 의미 | 예시 |
|----|------|------|
| `'price'` | 정액 할인 | 3,000원 할인 |
| `'percent'` | 정률 할인 | 10% 할인 |

### useable_yn (사용 가능 여부)
| 값 | 의미 |
|----|------|
| `'Y'` | 사용 가능 |
| `'N'` | 사용 불가 (만료/소진) |

### addable_yn (장바구니 추가 가능)
| 값 | 의미 |
|----|------|
| `'Y'` | 장바구니에서 적용 가능 |
| `'N'` | 자동 적용만 |

### multiple_yn (중복 사용 가능)
| 값 | 의미 |
|----|------|
| `'Y'` | 다른 쿠폰과 중복 사용 가능 |
| `'N'` | 단독 사용만 |

### vip_yn (VIP 전용)
| 값 | 의미 |
|----|------|
| `'Y'` | VIP 회원 전용 |
| `'N'` | 전체 회원 사용 가능 |

### daily_yn (데일리 쿠폰)
| 값 | 의미 |
|----|------|
| `'Y'` | 매일 초기화되는 한정 쿠폰 |
| `'N'` | 일반 쿠폰 |

---

## 🎁 coupon (발급된 쿠폰)

### use_yn (사용 여부)
| 값 | 의미 |
|----|------|
| `'Y'` | 사용 완료 |
| `'N'` | 미사용 |

---

## 📦 coupon_pack (쿠폰팩)

### status (상태)
| 값 | 의미 |
|----|------|
| `'ACTIVE'` | 활성 |
| `'INACTIVE'` | 비활성 |

---

## 💰 point (포인트)

### status (포인트 상태)
| 값 | 의미 |
|----|------|
| `'add'` | 적립 |
| `'use'` | 사용 |
| `'expire'` | 만료 |
| `'cancel'` | 취소/회수 |

### type (포인트 유형)
| 값 | 의미 |
|----|------|
| `'review'` | 리뷰 작성 적립 |
| `'order'` | 주문 적립 |
| `'event'` | 이벤트 적립 |
| `'admin'` | 관리자 지급 |

---

## 🐕 pet (반려동물)

### sex (성별)
| 값 | 의미 |
|----|------|
| `'남'` | 수컷 |
| `'암'` | 암컷 |

### neutered_yn (중성화 여부)
| 값 | 의미 |
|----|------|
| `'Y'` | 중성화 완료 |
| `'N'` | 중성화 안함 |

### disease_yn (질병 보유)
| 값 | 의미 |
|----|------|
| `'Y'` | 질병 있음 |
| `'N'` | 질병 없음 |

### allergy_yn (알레르기 보유)
| 값 | 의미 |
|----|------|
| `'Y'` | 알레르기 있음 |
| `'N'` | 알레르기 없음 |

---

## 🐾 pet_kind (반려동물 종류)

| no | kind_name |
|----|-----------|
| 1 | 강아지 |
| 2 | 고양이 |

---

## ⭐ review (리뷰)

### best_yn (베스트 리뷰)
| 값 | 의미 |
|----|------|
| `'Y'` | 베스트 리뷰 선정 |
| `'N'` | 일반 리뷰 |

### score (평점)
| 값 | 의미 |
|----|------|
| 1~5 | 별점 (1: 최저, 5: 최고) |

---

## 🚨 declaration (신고)

### type (신고 유형)
| 값 | 의미 |
|----|------|
| `'review'` | 리뷰 신고 |

### complete_yn (처리 완료)
| 값 | 의미 |
|----|------|
| `'Y'` | 처리 완료 |
| `'N'` | 미처리 |

---

## 📷 photos (사진)

### type (사진 유형)
| 값 | 의미 |
|----|------|
| `'item_qna'` | 상품 Q&A 첨부 |
| `'review'` | 리뷰 첨부 |

---

## 💳 card (카드)

### main_card_yn (주카드 여부)
| 값 | 의미 |
|----|------|
| `'Y'` | 주 결제 카드 |
| `'N'` | 보조 카드 |

### use_yn (사용 여부)
| 값 | 의미 |
|----|------|
| `'Y'` | 사용 가능 |
| `'N'` | 사용 중지 |

### payment_yn (결제 가능)
| 값 | 의미 |
|----|------|
| `'Y'` | 결제 가능 |
| `'N'` | 결제 불가 (카드 오류 등) |

---

## 🛒 cart (장바구니)

### subscribe_yn (정기구독 여부)
| 값 | 의미 |
|----|------|
| `'Y'` | 정기구독으로 담음 |
| `'N'` | 일반 구매로 담음 |

---

## 📱 member_action_log (회원 활동 로그)

### type (액션 타입)
| 값 | 의미 |
|----|------|
| `'app_open'` | 앱 실행 |
| `'item_view'` | 상품 조회 |
| `'cart_add'` | 장바구니 추가 |
| `'order'` | 주문 |

---

## 📨 order_sms_history (SMS 발송)

### result (발송 결과)
| 값 | 의미 |
|----|------|
| `'success'` | 발송 성공 |
| `'fail'` | 발송 실패 |

### type (발송 주체)
| 값 | 의미 |
|----|------|
| `'admin'` | 관리자 발송 |
| `'system'` | 시스템 자동 발송 |
| `'user'` | 사용자 요청 발송 |

---

## 📊 자주 사용하는 필터 조합

### 활성 회원 조회
```sql
WHERE del_yn = 'N'
```

### 판매 중인 상품
```sql
WHERE del_yn = 'N' AND view_yn = 'Y'
```

### 정상 진행 중인 주문
```sql
WHERE status NOT IN ('2', '7')  -- 취소, 환불 제외
```

### 사용 가능한 쿠폰
```sql
WHERE use_yn = 'N'
  AND use_end_time >= NOW()
  AND del_yn = 'N'
```

### 활성 구독
```sql
WHERE subscribe_yn = 'Y' AND end_yn = 'N'
```

### 활성 구독 상품
```sql
WHERE end_yn = 'N'
```

